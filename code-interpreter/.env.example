# Copy to .env (gitignored) for local dev, OR set these as Railway/Fly env vars.
# NEVER commit the real values.
E2B_API_KEY=e2b_your_key_here
SUPABASE_JWT_SECRET=your_supabase_jwt_secret_from_project_settings_api
ALLOWED_ORIGINS=https://techedu.icu,http://localhost:5173
SANDBOX_TIMEOUT=60
MAX_CODE=20000
